unbind r 
bind r source-file ~/.tmux.conf

#change leader
set -g prefix C-p
unbind C-b
bind C-p send-prefix

set-option -sa terminal-overrides ',xterm-256color:Tc'
set -g mouse on
set-window-option -g pane-base-index 1
set-option -g renumber-windows on
set -g base-index 1              # start indexing windows at 1 instead of 0
set -g pane-base-index 1         # start pane index from 1
set -g detach-on-destroy off     # don't exit from tmux when closing a session
set -g escape-time 0             # zero-out escape time delay
set -g history-limit 1000000     # increase history size (from 2,000)
set -g renumber-windows on       # renumber all windows when any window is closed
set -g set-clipboard on          # use system clipboard
set -g allow-passthrough on      # let terminal emulator detect URLs through tmux
set -ga terminal-features ',xterm-256color:hyperlinks'  # pass OSC 8 hyperlinks to terminal
set -g status-position top       # macOS / darwin style
set -g default-terminal "${TERM}"
set -g pane-active-border-style 'fg=magenta,bg=default'
set -g pane-border-style 'fg=brightblack,bg=default'


set -g @catppuccin_window_left_separator ""
set -g @catppuccin_window_right_separator " "
set -g @catppuccin_window_middle_separator " █"
set -g @catppuccin_window_number_position "right"
set -g @catppuccin_window_default_fill "number"
set -g @catppuccin_window_default_text "#W"
set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_current_text "#W#{?window_zoomed_flag,(),}"
set -g @catppuccin_status_modules_right "directory date_time"
set -g @catppuccin_status_modules_left "session"
set -g @catppuccin_status_left_separator  " "
set -g @catppuccin_status_right_separator " "
set -g @catppuccin_status_right_separator_inverse "no"
set -g @catppuccin_status_fill "icon"
set -g @catppuccin_status_connect_separator "no"
set -g @catppuccin_directory_text "#{b:pane_current_path}"
set -g @catppuccin_meetings_text "#($HOME/.config/tmux/scripts/cal.sh)"
set -g @catppuccin_date_time_text "%H:%M"

#option+shift + arrrows for resize-pane
bind -n M-S-Up resize-pane -U 5 
bind -n M-S-Down resize-pane -D 5 
bind -n M-S-Left resize-pane -R 5
bind -n M-S-Right resize-pane -L 5
bind '"' split-window -v -c '#{pane_current_path}'
bind % split-window -h -c '#{pane_current_path}'

bind t new-window -c '#{pane_current_path}'

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'dreamsofcode-io/catppuccin-tmux'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
# session manager (fzf popup)
bind o display-popup -E -w 60% -h 60% "$HOME/.local/bin/tmux-sessions"
# quick terminal popup in current directory
bind y display-popup -E -w 80% -h 80% -d '#{pane_current_path}' "tmux set mouse on && $SHELL"

# tmux-resurrect: restore vim/nvim sessions
set -g @resurrect-strategy-nvim 'session'
set -g @resurrect-strategy-vim 'session'
# tmux-resurrect: capture pane contents
set -g @resurrect-capture-pane-contents 'on'
# tmux-resurrect: restore additional programs
set -g @resurrect-processes 'ssh claude node npm "npm run dev" python python3'

# tmux-continuum: auto-save every 15 minutes
set -g @continuum-save-interval '15'
# tmux-continuum: auto-restore on tmux start (disable if you prefer manual restore)
set -g @continuum-restore 'on'

run -b '~/.tmux/plugins/tpm/tpm'
